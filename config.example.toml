# QQ Bot 配置示例文件
# 复制为 config.toml 并按需修改。
# 优先级：TOML < 环境变量 < CLI 参数

# 私聊白名单，只接受这些用户的私聊。空数组 = 不限制
# allowed_private_users = [10001, 10002]
allowed_private_users = []

# ---------------------------------------------------------------------------
# 管理员（QQ号 → 昵称）
# 注入系统提示词（记忆中视为可信来源）+ 授权群管理工具（禁言、头衔等）
# ---------------------------------------------------------------------------
[admins]
# "123456789" = "管理员昵称"

# ---------------------------------------------------------------------------
# LLM 接入
# ---------------------------------------------------------------------------
[llm]
# Anthropic 兼容 API 地址
# 环境变量: LLM_BASE_URL | CLI: --llm-base-url
base_url = "http://127.0.0.1:34567/v1"

# API 密钥
# 环境变量: LLM_API_KEY | CLI: --llm-api-key
api_key = "sk-placeholder"

# 模型名称
# 环境变量: LLM_MODEL | CLI: --llm-model
model = "claude-sonnet-4-6"

# 单次回复最大 token 数
max_tokens = 1024

# ---------------------------------------------------------------------------
# 上下文窗口
# ---------------------------------------------------------------------------
[llm.context]
# 上下文窗口大小（token 数），超出后触发 compact
max_context_tokens = 1_000_000

# ---------------------------------------------------------------------------
# LLM 用量追踪
# ---------------------------------------------------------------------------
[llm.usage]
# 是否启用用量记录
enabled = true
# 单次调用耗时告警阈值（秒）
slow_threshold_s = 60.0

# ---------------------------------------------------------------------------
# 日志
# ---------------------------------------------------------------------------
[log]
dir = "storage/logs"

# ---------------------------------------------------------------------------
# 长期记忆
# ---------------------------------------------------------------------------
[memory]
# 记忆文件存储目录
dir = "storage/memories"
# 单条用户备忘录最大字符数
user_max_chars = 300
# 单条群备忘录最大字符数
group_max_chars = 500
# 备忘录索引最大行数
index_max_lines = 200
# 是否启用备忘录历史记录
history_enabled = true

# ---------------------------------------------------------------------------
# 人设与指令（soul 目录）
# ---------------------------------------------------------------------------
[soul]
# 存放 identities.md 和 instruction.md 的目录
dir = "soul"

# ---------------------------------------------------------------------------
# 上下文压缩
# ---------------------------------------------------------------------------
[compact]
# micro compact 触发比例（占 max_context_tokens）
micro_ratio = 0.6
# full compact 触发比例（占 max_context_tokens）
full_ratio = 0.8
# 连续压缩失败上限，超过后跳过
max_failures = 3
# cache_hit 低于此百分比时告警
cache_hit_warn = 90.0

# ---------------------------------------------------------------------------
# Dream 整理（离线摘要）
# ---------------------------------------------------------------------------
[dream]
# 是否启用 dream 整理
enabled = false
# 多久跑一次（小时）
interval_hours = 24
# 至少累积多少次 compact 后才触发
min_compacts = 5
# 单次 dream 最大轮次
max_rounds = 15

# ---------------------------------------------------------------------------
# 群聊
# ---------------------------------------------------------------------------
[group]
# 内存中保留的最大群消息条数
max_timeline_messages = 200

# 启动时从 NapCat 拉取的历史消息条数
history_load_count = 30

# 群聊白名单，只处理这些群的消息。空数组 = 不限制
# allowed_groups = [100001, 100002]
allowed_groups = []

# 非@消息的 debounce 等待时间（秒），等消息间隙后触发模型调用
debounce_seconds = 5.0

# 最多攒多少条消息强制触发模型调用
batch_size = 10

# ---------------------------------------------------------------------------
# NapCat HTTP API
# ---------------------------------------------------------------------------
[napcat]
# NapCat HTTP API 地址
# 环境变量: NAPCAT_API_URL
api_url = "http://localhost:29300"

# ---------------------------------------------------------------------------
# 多模态视觉（图片理解）
# ---------------------------------------------------------------------------
[vision]
# 总开关，关闭后所有图片降级为 [图片] 文字
enabled = true

# 单条消息最多处理的图片数，超出的用 [图片] 代替
max_images_per_message = 5

# 单次 API 请求累计图片上限（跨所有历史消息）
max_images_per_request = 15

# 图片缩放最大边像素
max_dimension = 768

# 图片缓存目录
cache_dir = "storage/image_cache"

# 缓存过期时间（小时）
cache_max_age_hours = 24


